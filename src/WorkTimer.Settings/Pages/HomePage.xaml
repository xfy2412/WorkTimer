<Page x:Class="WorkTimer_Settings.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer>
        <StackPanel Margin="24,16" Spacing="16">
            <TextBlock Text="个性化设置" Style="{StaticResource TitleTextBlockStyle}"/>

            <TextBox Header="悬停亮起延迟（秒）" Text="{x:Bind Settings.HoverDelaySeconds, Mode=TwoWay}"/>
            <TextBox Header="窗口宽度" Text="{x:Bind Settings.WindowWidth, Mode=TwoWay}"/>
            <TextBox Header="窗口高度" Text="{x:Bind Settings.WindowHeight, Mode=TwoWay}"/>
        </StackPanel>
    </ScrollViewer>
</Page>
