<Page x:Class="WorkTimer_Settings.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Page.Resources>
        <Style x:Key="SettingsCard" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="0,0,0,1"/>
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,16,0,8"/>
        </Style>
        <Style x:Key="CardTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="CardDesc" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
    </Page.Resources>

    <ScrollViewer>
        <StackPanel Margin="16,8">
            <TextBlock Text="个性化" Style="{StaticResource SectionTitle}"/>

            <Border Style="{StaticResource SettingsCard}">
                <Grid ColumnDefinitions="*,Auto">
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="悬停亮起延迟" Style="{StaticResource CardTitle}"/>
                        <TextBlock Text="鼠标悬停悬浮窗后自动亮起的等待时间" Style="{StaticResource CardDesc}"/>
                    </StackPanel>
                    <Slider Grid.Column="1" x:Name="HoverSlider" Width="140" Minimum="0.5" Maximum="5"
                            TickFrequency="0.5" StepFrequency="0.5"/>
                </Grid>
            </Border>

            <Border Style="{StaticResource SettingsCard}">
                <Grid ColumnDefinitions="*,Auto">
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="默认透明度" Style="{StaticResource CardTitle}"/>
                        <TextBlock Text="悬浮窗在未交互时的透明度" Style="{StaticResource CardDesc}"/>
                    </StackPanel>
                    <Slider Grid.Column="1" x:Name="OpacitySlider" Width="140" Minimum="0.05" Maximum="0.5"
                            TickFrequency="0.05" StepFrequency="0.05"/>
                </Grid>
            </Border>

            <Button x:Name="ReloadBtn" Content="重载设置" HorizontalAlignment="Right"
                    Margin="0,12,0,0" Padding="20,8"
                    Click="ReloadBtn_Click"/>
        </StackPanel>
    </ScrollViewer>
</Page>
